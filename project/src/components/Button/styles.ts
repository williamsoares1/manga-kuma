import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        backgroundColor:'blue',
        padding: 15,
        borderRadius: 7,
        alignItems: 'center',
        marginVertical: 20
    },
    buttonText: {
        color: '#FFF',
        fontSize: 17,
        fontWeight: 'bold'
    }
})