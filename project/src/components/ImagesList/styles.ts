import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    imageContainer: {
        maxWidth: '100%',
        paddingHorizontal: 10
    },
    image: {
        flex: 1,
        width: '100%',
        resizeMode: 'contain',
    },
});
