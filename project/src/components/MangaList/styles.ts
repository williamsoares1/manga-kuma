import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 10
    },
    surprising:{
        alignSelf: 'center',
        height: '100%',
        marginVertical: 50,
        fontSize: 20,
        color: '#fff'
    }
})